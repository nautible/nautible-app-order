<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>jp.co.ogis-ri.nautible.app</groupId>
  <artifactId>nautible-app-ms-order-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <properties>
    <compiler-plugin.version>3.8.1</compiler-plugin.version>
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <maven.compiler.source>11</maven.compiler.source>
    <maven.compiler.target>11</maven.compiler.target>
    <org.mapstruct.version>1.4.1.Final</org.mapstruct.version>
    <swagger-annotations.version>1.6.2</swagger-annotations.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <quarkus-plugin.version>1.13.6.Final</quarkus-plugin.version>
    <quarkus.platform.artifact-id>quarkus-universe-bom</quarkus.platform.artifact-id>
    <quarkus.platform.group-id>io.quarkus</quarkus.platform.group-id>
    <quarkus.platform.version>1.13.6.Final</quarkus.platform.version>
    <surefire-plugin.version>3.0.0-M5</surefire-plugin.version>
    <openapi.generator.maven.plugin.version>5.0.0</openapi.generator.maven.plugin.version>
    <nautible.app.order.groupId>jp.co.ogis-ri.nautible.app</nautible.app.order.groupId>
    <nautible.app.order.name>nautible-app-ms-order</nautible.app.order.name>
    <nautible.app.order.version>1.0.0-SNAPSHOT</nautible.app.order.version>
    <dapr.sdk.version>1.5.0</dapr.sdk.version>
    <okhttp.version>4.9.0</okhttp.version>
    <okio.version>2.8.0</okio.version>
    <jandex.maven.plugin.version>1.1.0</jandex.maven.plugin.version>
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.dapr</groupId>
        <artifactId>dapr-sdk</artifactId>
        <version>${dapr.sdk.version}</version>
      </dependency>
      <!-- struct -->
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${org.mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct-processor</artifactId>
        <version>${org.mapstruct.version}</version>
        <scope>provided</scope>
      </dependency>
      <!-- https://mvnrepository.com/artifact/io.swagger/swagger-annotations -->
      <dependency>
        <groupId>io.swagger</groupId>
        <artifactId>swagger-annotations</artifactId>
        <version>${swagger-annotations.version}</version>
      </dependency>
      <!-- quarkusとdapr-sdkでconflict。 現在利用しているquarkusのmoduleは利用していないので、dapr-sdkの依存バージョンでoverride -->
      <dependency>
         <groupId>com.squareup.okhttp3</groupId>
         <artifactId>okhttp</artifactId>
         <version>${okhttp.version}</version>
      </dependency>
      <dependency>
        <groupId>com.squareup.okio</groupId>
        <artifactId>okio</artifactId>
        <version>${okio.version}</version>
      </dependency>
      <dependency>
        <groupId>${nautible.app.order.groupId}</groupId>
        <artifactId>${nautible.app.order.name}-core</artifactId>
        <version>${nautible.app.order.version}</version>
      </dependency>
      <dependency>
        <groupId>${nautible.app.order.groupId}</groupId>
        <artifactId>${nautible.app.order.name}-aws</artifactId>
        <version>${nautible.app.order.version}</version>
      </dependency>
      <dependency>
        <groupId>${nautible.app.order.groupId}</groupId>
        <artifactId>${nautible.app.order.name}-azure</artifactId>
        <version>${nautible.app.order.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.jboss.jandex</groupId>
          <artifactId>jandex-maven-plugin</artifactId>
          <version>${jandex.maven.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>io.quarkus</groupId>
          <artifactId>quarkus-maven-plugin</artifactId>
          <version>${quarkus-plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler-plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.openapitools</groupId>
          <artifactId>openapi-generator-maven-plugin</artifactId>
          <version>${openapi.generator.maven.plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${surefire-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <profiles>
    <!-- 
      最新のmvn(3.8.5)を利用した場合、quarkus-maven-pluginではコマンドライン引数(-P xxx)でのプロファイル指定が有効とならない。
      system propertyでのプロファイルの有効化が機能するのでsystem propertyを利用する。
    -->
    <profile>
      <id>default</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <modules>
        <module>nautible-app-ms-order-core</module>
        <module>nautible-app-ms-order-aws</module>
        <module>nautible-app-ms-order-azure</module>
        <module>nautible-app-ms-order-build</module>
      </modules>
    </profile>
    <profile>
      <id>aws</id>
      <activation>
        <property>
          <name>cloud</name>
          <value>aws</value>
        </property>
      </activation>
      <modules>
        <module>nautible-app-ms-order-core</module>
        <module>nautible-app-ms-order-aws</module>
        <module>nautible-app-ms-order-build</module>
      </modules>
    </profile>
    <profile>
      <id>azure</id>
      <activation>
        <property>
          <name>cloud</name>
          <value>azure</value>
        </property>
      </activation>
      <modules>
        <module>nautible-app-ms-order-core</module>
        <module>nautible-app-ms-order-azure</module>
        <module>nautible-app-ms-order-build</module>
      </modules>
    </profile>
  </profiles>
</project>
